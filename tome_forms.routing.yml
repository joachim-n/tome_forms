tome_forms.tome_form_handler_export:
  path: '/tome-form-handler/{tome_form}'
  defaults:
    _title: Tome form handler
    _controller: '\Drupal\tome_forms\Controller\TomeFormHandlerExportController::content'
  requirements:
    # TODO: Consider access control that checks for either an admin permission,
    # or the Tome request attribute.
    _access: 'TRUE'
